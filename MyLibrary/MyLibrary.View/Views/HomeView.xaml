<UserControl x:Class="MyLibrary.View.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyLibrary.View.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#6CA651">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Grid.RowSpan="3" CornerRadius="20" Background="#839705" Visibility="{Binding ShowClientsCount}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" Text="تعداد کاربران"/>

                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" HorizontalAlignment="Center" Text="{Binding ClientsCount}"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="3" Grid.RowSpan="3" CornerRadius="20" Background="#839705" Visibility="{Binding ShowDilayedLoanCount}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" Text="تعداد امانت عقب افتاده"/>

                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" HorizontalAlignment="Center"  Text="{Binding DilayedLoanCount}"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Grid.Column="5" Grid.ColumnSpan="3" Grid.RowSpan="3" CornerRadius="20" Background="#839705" Visibility="{Binding ShowBooksCount}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" Text="تعداد کتاب ها"/>

                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" HorizontalAlignment="Center"  Text="{Binding BooksCount}"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="6" Grid.Column="5" Grid.ColumnSpan="3" Grid.RowSpan="3" CornerRadius="20" Background="#839705" Visibility="{Binding ShowLoansCount}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" Text="تعداد کل امانت ها"/>

                <TextBlock Padding="20" FontWeight="Bold" FontSize="25" HorizontalAlignment="Center"  Text="{Binding LoansCount}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
